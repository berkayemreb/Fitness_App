import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: '#0091ea',
        padding: 6,
        marginTop: 5,
        marginHorizontal: 15,
        borderRadius: 15,
    },
    text: {
        textAlign: 'center',
        color: '#ffffff',
        fontWeight: '600',
    }
})