import React from "react";
import { TouchableOpacity } from 'react-native';

const Button = () => {
    return (
        <TouchableOpacity>
            <Text></Text>
        </TouchableOpacity>
    )
}

export default Button;